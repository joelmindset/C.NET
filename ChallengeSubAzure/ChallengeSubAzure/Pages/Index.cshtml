@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
  @{
    var greeting = "Hello,";
    var weekDay = DateTime.Now.DayOfWeek;
    var greet = greeting + " The Day of the week is: " + weekDay;
  }
  <h2 class="display-4">@greet</h2>
  <p>Current time is @ViewData["TimeStamp"].</p>
</div>
 